[project]
name = "percentile"
version = "0.1.0"
description = "GMAT pattern, error, mastery and adaptive-training engine"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]
learning = [
  "pyBKT>=1.4",
  "py-fsrs>=1.0",
]
cat = [
  "catsim>=0.20",
  "py-irt>=0.6",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
